#!/bin/sh
#
# Forcefully reassociate DEV on different networks
#
DEV=wlan0
set -x

down() {
  sudo ip link set $1 down
  sudo ifconfig $1 0.0.0.0
}
down eth0
down $DEV

sudo pkill dhclient
sudo dhclient $DEV
