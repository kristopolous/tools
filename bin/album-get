#!/bin/bash
base=$1
shift

[ -e $base ] || mkdir $base
cd $base

for i in $(curl -s https://$base.bandcamp.com/ | grep -Po '(?<=album\/)[^?"]*' ); do
  (
    [ -e $i ] || mkdir $i
    cd $i
    url=https://$base.bandcamp.com/album/$i
    echo -e "\n\n$url\n\n"
    youtube-dl $url
  )
done
